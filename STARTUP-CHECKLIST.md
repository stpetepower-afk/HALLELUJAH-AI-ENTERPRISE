# STARTUP-CHECKLIST.md

Step-by-step setup guide

1. Clone the repo
2. Copy `.env.example` to `.env` and populate secrets
3. Install dependencies (npm install)
4. Run `npm run dev` and verify app boots
5. Configure CI (GitHub Actions) and required secrets
6. Enable branch protections and required status checks

